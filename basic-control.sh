#!/bin/bash

echo a really long line that we want to split for \
     aesthetic reasons

echo effectively ; echo multiple ; echo lines

echo any && echo failures && ls cancel && echo following commands

ls failure || echo is skipped until something works || echo then all else is ignored
